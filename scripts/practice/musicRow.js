const createJumpStop = (name, genre, age) => {
    const jumpArtist = {
        name: name,
        genre: genre,
        age: age
    }
    return jumpArtist
}

const createChatten =  (name, genre, age ) => {
    const chattenArtist = {
        name: name,
        genre: genre,
        age: age
    }
    return chattenArtist
}

const createPolar = (name, genre, age) => {
    const polarArtist = {
        name: name,
        genre: genre,
        age: age
    }
    return polarArtist
}

export {createJumpStop, createChatten, createPolar}